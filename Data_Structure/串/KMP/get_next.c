#include <stdio.h>
#include <string.h>
int main()
{
    int[255] next;
    char[] T = "abcdaab";
    int len = sizeof(T) / sizeof(T[0]);
    int i, j;
    i = 1;
    k = 0;
    while (i <len)
    {
       if(k==0||T[i])
    }
}